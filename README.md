# Nexus v13.1 — Render Deploy Ready

- `render.yaml` pronto para Blueprint.
- SQLite e uploads persistem no mesmo disco do Render.
- `DATABASE_URL` e `NEXUS_UPLOAD_DIR` configuráveis.
- Call v12.0.2 preservada.
- Local: http://localhost:5231

Leia `PUBLICAR-RENDER.md`.
