{
  "name": "nexus-v13-1-render-deploy-ready",
  "version": "13.1.0",
  "private": true,
  "description": "Nexus v13.1 - pronto para deploy no Render com SQLite/uploads persistentes",
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "render:build": "npm ci && npx prisma generate",
    "render:start": "mkdir -p /opt/render/project/src/storage/uploads && npx prisma db push --skip-generate && node server/index.js"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.14.0",
    "socket.io": "^4.8.1",
    "zod": "^4.0.17",
    "multer": "^2.0.2"
  }
}